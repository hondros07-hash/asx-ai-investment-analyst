# AXÍA V23.7.4 — Company Intelligence Data Pipeline Repair

Baseline: V23.7.3.

- Retrieved news is shown as context only, never promoted to verified causal evidence.
- Preserves verified event/exposure gate; adds headline context and explicit provenance diagnostics.
- Reads SEC_USER_AGENT from Streamlit secrets when absent in environment. Use a genuine application/contact identifier.
- SEC exceptions preserve explicit upstream-error diagnostics.
- News grid uses two columns with source on its own row to prevent overlapping.
- Technical diagnostics remain collapsed by default.

Note: A valid SEC_USER_AGENT and working network access are required for live SEC retrieval. Verified exposure/event mappings must come from sourced structured inputs; this build does not invent them.
