# AXÍA V23.7.3 — Overview Intelligence & Layout Stabilisation

Baseline: V23.7.2 full project. Existing banner, sidebar, navigation, authentication and valuation services retained.

- Company Intelligence tries all structured events rather than allowing the first unmatched event to mask a later match; evidence diagnostics visible when unmapped. Headline-only inference remains prohibited.
- SEC EDGAR requires a real SEC_USER_AGENT, distinguishes missing configuration, mapping/transport failures and validates the returned issuer ticker. Set SEC_USER_AGENT in Streamlit secrets/environment to `AXIA Research your-real-contact@yourdomain.com` (replace with actual contact). See SEC developer fair-access guidance.
- Recent News grid clips and ellipsizes headlines/source without overlap.
- Catalyst header has one functional View all button in a fixed-height card; removed detached blue button and inert decorative link.
- Scoped external spacing replaces broad Streamlit parent gap override.

Verification: 15 targeted tests passed; Python compilation passed. Full legacy suite fails on a pre-existing stale test expecting retired `v21307_nav_ann` while the live app uses `v21310_nav_ann`. Live provider and browser rendering remain unverified.
