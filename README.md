# Chrímata V19.8.5 — Fixed Full-Width Terminal UI

This build replaces the fragile negative-offset landing-page shell with a fixed viewport-wide Parthenon hero and an explicitly visible fixed desktop sidebar. The banner begins at the absolute top of the app and spans the full browser width; the sidebar begins directly beneath it.

It retains the V19.8.x global market overview and all existing research functionality.
