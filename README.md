# Chrímata V19.8.7 — Aligned Terminal Landing Page

Deployment-clean build. The fixed full-width Parthenon banner remains at the top. The desktop sidebar is fixed below it at 214px, and the main application canvas now starts explicitly to the right of the sidebar instead of rendering underneath it. This fixes the clipped search, market title, first metric card and chart controls visible in V19.8.6.
