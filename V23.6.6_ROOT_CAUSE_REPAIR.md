AXÍA V23.6.6 — Valuation Pipeline Root-Cause Repair
Baseline: user-uploaded V23.6.5 full project.
Corrects the inconsistency where primary extraction recognized normalized/variant cash-flow rows but trailing-period extraction used exact-only row matches. Both statement orientations are supported. Adds explicit resolved row labels to valuation audit and three regression tests. Existing eligibility rules remain: missing CapEx is not zero, negative FCF is not silently treated as missing, and a full DCF still requires other verified inputs. Live ticker/provider response testing is not included.
